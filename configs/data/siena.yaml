# Data configuration for Siena dataset
h5_path: "/siena_windows.h5"
femba_model_path: "femba_base.pt"  # Path to pretrained FEMBA weights
output_dir: "outputs/checkpoints"

val_ratio: 0.2
num_workers: 0
pin_memory: true
